const DarkButton = (props) => {
    return (
        <>
            <button className="dark-btn">
            <p>{props.text}</p>
            </button>
        </>
    )
}

export default DarkButton;